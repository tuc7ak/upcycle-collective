// Single source of truth for vendor names — edit only here. Both
// vendor-checkin.html (generates the QR codes) and dashboard.html (shows
// per-vendor visit counts) load this file, so they can never drift apart.
const TUC_VENDORS = ['POUCH', 'FIGURINE', 'PEWANI', 'LANDIK', 'MAKENTAKE'];
